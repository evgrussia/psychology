# Синхронизация токенов из Figma

**Версия:** 1.0  
**Дата создания:** 13 января 2026

---

## 📋 Описание

Этот документ описывает процесс синхронизации дизайн-токенов из Figma в проект.

---

## 🔄 Процесс синхронизации

### Вариант 1: Figma Tokens Plugin (Рекомендуется)

1. **Установите плагин Figma Tokens**
   - Откройте Figma
   - Перейдите в Plugins → Browse plugins
   - Найдите "Figma Tokens" и установите

2. **Экспорт токенов из Figma**
   - Откройте [Figma Design System](https://www.figma.com/make/ls1ACoHXpuzTb3hkMuGrsB/Emotional-Balance-Design-System?t=aP31NKbERGrs98Ho-1)
   - Запустите плагин Figma Tokens
   - Выберите токены для экспорта
   - Экспортируйте в JSON формат

3. **Конвертация в TypeScript**
   - Используйте скрипт `convert-tokens.js` (см. ниже)
   - Или вручную обновите файлы в `design-system/tokens/`

### Вариант 2: Ручная синхронизация

1. Откройте раздел "Design Tokens" в Figma
2. Скопируйте значения токенов
3. Обновите соответствующие файлы:
   - `design-system/tokens/colors.ts`
   - `design-system/tokens/typography.ts`
   - `design-system/tokens/spacing.ts`
   - `design-system/tokens/effects.ts`
4. Обновите CSS файлы:
   - `design-system/tokens/colors.css`

---

## 🛠️ Скрипт конвертации (пример)

Создайте файл `design-system/scripts/convert-tokens.js`:

```javascript
/**
 * Скрипт для конвертации токенов из Figma Tokens JSON в TypeScript
 * 
 * Использование:
 * node design-system/scripts/convert-tokens.js <input.json> <output-dir>
 */

const fs = require('fs');
const path = require('path');

function convertTokens(inputFile, outputDir) {
  const tokens = JSON.parse(fs.readFileSync(inputFile, 'utf8'));
  
  // Конвертация цветов
  if (tokens.color) {
    const colors = {};
    // Логика конвертации...
    
    const colorsTS = `export const colors = ${JSON.stringify(colors, null, 2)};`;
    fs.writeFileSync(path.join(outputDir, 'colors.ts'), colorsTS);
  }
  
  // Аналогично для других токенов...
}

const inputFile = process.argv[2];
const outputDir = process.argv[3] || path.join(__dirname, '../tokens');

convertTokens(inputFile, outputDir);
```

---

## 📝 Чеклист синхронизации

При обновлении токенов из Figma:

- [ ] Экспортированы токены из Figma
- [ ] Обновлены файлы в `design-system/tokens/`
- [ ] Обновлены CSS Custom Properties
- [ ] Проверена совместимость с существующим кодом
- [ ] Обновлена документация в `docs/UI-Kit-Design-System.md`
- [ ] Протестированы компоненты с новыми токенами
- [ ] Создан коммит с описанием изменений

---

## 🔍 Проверка изменений

После синхронизации проверьте:

1. **Визуальная проверка**
   - Откройте компоненты в Storybook или приложении
   - Убедитесь, что цвета и размеры соответствуют Figma

2. **Автоматическая проверка**
   - Запустите тесты компонентов
   - Проверьте линтер и TypeScript ошибки

3. **Документация**
   - Обновите примеры в `design-system/components/EXAMPLES.md`
   - Обновите `docs/UI-Kit-Design-System.md` если изменились значения

---

## 📚 Дополнительные ресурсы

- [Figma Tokens Plugin Documentation](https://docs.tokens.studio/)
- [Design Tokens Format](https://tr.designtokens.org/format/)
- [Figma API Documentation](https://www.figma.com/developers/api)

---

**Последнее обновление:** 13 января 2026
