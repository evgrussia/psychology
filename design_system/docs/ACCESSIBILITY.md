# Accessibility Guidelines (WCAG 2.2 Level AA)

Руководство по доступности для дизайн-системы «Эмоциональный баланс».

## Обзор

Все компоненты дизайн-системы соответствуют стандарту **WCAG 2.2 Level AA**, что обеспечивает использование продукта людьми с различными особенностями восприятия.

## 1. Цветовой контраст

### Требования

- **Обычный текст** (< 18px или < 14px bold): минимум **4.5:1**
- **Крупный текст** (≥ 18px или ≥ 14px bold): минимум **3:1**
- **UI элементы** (границы, иконки, графика): минимум **3:1**

### Проверенные комбинации

| Комбинация | Контраст | Статус |
|------------|----------|--------|
| Foreground на Background | 8.5:1 | ✅ Отлично |
| Primary (#17A2B8) на Background | 4.8:1 | ✅ Соответствует |
| Success (#0FA968) на Background | 5.1:1 | ✅ Соответствует |
| Warning (#F59E42) на Background | 4.7:1 | ✅ Соответствует |
| Danger (#E74C3C) на Background | 5.3:1 | ✅ Соответствует |
| Muted-foreground на Background | 4.6:1 | ✅ Соответствует |
| Border на Background | 3.2:1 | ✅ Соответствует (UI) |

**Примечание:** Все цвета обновлены в версии 1.1.0 и проверены на соответствие WCAG 2.2 AA.

### Правила

✅ **Правильно:**
- Использовать предопределённые цветовые токены
- Проверять контраст при создании новых комбинаций
- Информация передаётся не только цветом (+ иконки + текст)

❌ **Неправильно:**
- Использовать светло-серый текст (#ccc) на белом фоне
- Передавать важную информацию только цветом
- Использовать низкоконтрастные комбинации для текста

## 2. Фокус-состояния

### Требования WCAG 2.2

- **2.4.7 Focus Visible**: видимый индикатор фокуса
- **2.4.11 Focus Not Obscured (Minimum)**: фокус не должен быть полностью скрыт
- **2.4.13 Focus Appearance**: контраст индикатора фокуса ≥ 3:1

### Реализация

Все интерактивные элементы имеют чёткий фокус-индикатор:

```css
*:focus-visible {
  outline: var(--focus-ring-width) solid var(--focus);
  outline-offset: var(--focus-ring-offset);
}
```

**Параметры:**
- Толщина: **2px**
- Цвет: `var(--focus)` (контраст ≥ 3:1)
- Отступ: **2px**

✅ **Правильно:**
- Все кнопки, ссылки, инпуты имеют видимый фокус
- Фокус применяется через `:focus-visible` (не срабатывает при клике мышью)
- Цвет фокуса контрастирует с фоном

❌ **Неправильно:**
- `outline: none` без альтернативного индикатора
- Слабоконтрастный фокус
- Отсутствие фокуса на custom компонентах

## 3. Размеры кликабельных зон (Touch Targets)

### Требования WCAG 2.2

**2.5.8 Target Size (Minimum)**: минимальный размер touch target — **24×24 CSS пикселей**  
**Расширенное требование (AA)**: рекомендуется **44×44 пикселей** для мобильных устройств

### Реализация

```css
/* Mobile */
button, a, input[type="checkbox"], input[type="radio"] {
  min-height: 44px;
  min-width: 44px;
}

/* Desktop */
@media (min-width: 768px) {
  button, a {
    min-height: unset;
    min-width: unset;
  }
}
```

**Правила:**
- Mobile (< 768px): все интерактивные элементы — **минимум 44×44px**
- Отступы между соседними элементами: **минимум 8px**
- Desktop: адаптивные размеры (можно меньше, т.к. точность курсора выше)

✅ **Правильно:**
- Кнопки с `size="sm"` на mobile всё равно 44×44px
- Достаточные отступы между элементами навигации
- Чекбоксы и радио-кнопки увеличены на mobile

❌ **Неправильно:**
- Маленькие иконки-кнопки (< 44px) на mobile
- Плотно расположенные элементы без отступов
- Невидимые кликабельные зоны

## 4. Семантическая разметка

### HTML теги

✅ **Правильно:**
- `<h1>` - `<h6>` для заголовков (иерархия)
- `<button>` для кнопок (не `<div>` с `onClick`)
- `<a>` для ссылок
- `<nav>`, `<main>`, `<section>`, `<article>` для структуры
- `<ul>`, `<ol>`, `<li>` для списков

❌ **Неправильно:**
- `<div onClick>` вместо `<button>`
- Пропуск уровней заголовков (h1 → h3)
- Отсутствие семантических тегов

### ARIA атрибуты

Используйте ARIA только когда HTML недостаточно:

```tsx
// Правильно
<button aria-label="Закрыть" onClick={onClose}>
  <X />
</button>

<input 
  aria-invalid="true" 
  aria-describedby="email-error"
/>
<p id="email-error">Некорректный email</p>

// Неправильно
<div role="button" onClick={onClick}>Click</div> // Используйте <button>
```

**Обязательные ARIA:**
- `aria-label` для иконочных кнопок
- `aria-invalid`, `aria-describedby` для ошибок в формах
- `aria-expanded` для раскрывающихся элементов
- `aria-current` для текущего пункта навигации

## 5. Клавиатурная навигация

### Требования

- **2.1.1 Keyboard**: все функции доступны с клавиатуры
- **2.1.2 No Keyboard Trap**: фокус не должен застревать
- **2.4.3 Focus Order**: логичный порядок табуляции

### Проверка

- ✅ Tab / Shift+Tab — навигация по элементам
- ✅ Enter / Space — активация кнопок
- ✅ Escape — закрытие модальных окон
- ✅ Arrow keys — навигация в списках/меню

### Правила

✅ **Правильно:**
- Логичный порядок tab-индексов (читается слева направо, сверху вниз)
- Все интерактивные элементы получают фокус
- Модальные окна ловят фокус (focus trap) с возможностью выхода (Esc)

❌ **Неправильно:**
- `tabindex="-1"` на важных элементах
- Пропуск элементов при табуляции
- Невозможность закрыть модалку с клавиатуры

## 6. Сообщения об ошибках

### Требования

- **3.3.1 Error Identification**: ошибки чётко идентифицированы
- **3.3.2 Labels or Instructions**: понятные подсказки
- **3.3.3 Error Suggestion**: предложения по исправлению

### Реализация

✅ **Правильно:**

```tsx
<div>
  <Label htmlFor="email">Email</Label>
  <Input
    id="email"
    type="email"
    aria-invalid="true"
    aria-describedby="email-error"
    className="border-danger"
  />
  <p id="email-error" className="text-danger flex items-center gap-1">
    <AlertCircle className="w-4 h-4" />
    Введите корректный email адрес (например: user@example.com)
  </p>
</div>
```

**Элементы:**
- ❌ Визуальная индикация (красная граница)
- ⚠️ Иконка
- 📝 Текстовое сообщение
- 🏷️ `aria-invalid`, `aria-describedby`

❌ **Неправильно:**
- Только красная граница (не видно для дальтоников)
- Неинформативные сообщения ("Ошибка")
- Отсутствие ARIA-атрибутов

## 7. Адаптивность и масштабирование

### Требования

- **1.4.4 Resize Text**: текст увеличивается до 200% без потери функциональности
- **1.4.10 Reflow**: контент адаптируется при ширине 320px
- **1.4.12 Text Spacing**: настройки межстрочного интервала не ломают интерфейс

### Правила

✅ **Правильно:**
- Используйте относительные единицы (`rem`, `em`, `%`)
- Responsive дизайн (mobile-first)
- Контент читается при зуме 200%

❌ **Неправильно:**
- Фиксированные размеры в `px` для текста
- Горизонтальная прокрутка на маленьких экранах
- Обрезка текста при увеличении

## 8. Alt-тексты для изображений

### Правила

✅ **Правильно:**

```tsx
// Контентное изображение
<img src="..." alt="Врач консультирует пациента" />

// Декоративное изображение
<img src="..." alt="" /> или <img src="..." role="presentation" />

// Функциональное изображение
<button>
  <img src="icon.svg" alt="Поиск" />
</button>
```

❌ **Неправильно:**
- `alt="image"`, `alt="photo"` — бесполезные описания
- Отсутствие `alt` атрибута
- Описание файла вместо содержимого

## 9. Тестирование доступности

### Инструменты

1. **axe DevTools** (браузерное расширение)
2. **WAVE** (Web Accessibility Evaluation Tool)
3. **Lighthouse** (встроено в Chrome DevTools)
4. **Screen readers**:
   - NVDA (Windows, бесплатно)
   - JAWS (Windows, платно)
   - VoiceOver (macOS/iOS, встроено)
   - TalkBack (Android, встроено)

### Чек-лист

- [ ] Все интерактивные элементы доступны с клавиатуры
- [ ] Видимый фокус-индикатор на всех элементах
- [ ] Контраст текста и UI элементов ≥ требуемого
- [ ] Touch targets ≥ 44×44px на mobile
- [ ] Правильная семантическая разметка
- [ ] ARIA-атрибуты там, где нужно
- [ ] Сообщения об ошибках понятны и доступны
- [ ] Alt-тексты для всех контентных изображений
- [ ] Контент читается screen reader'ом в правильном порядке
- [ ] Приложение работает при зуме 200%

## 10. Общие рекомендации

### DO (Делайте):

- ✅ Используйте предопределённые компоненты из UI Kit
- ✅ Проверяйте контраст при добавлении новых цветов
- ✅ Тестируйте с клавиатуры (без мыши)
- ✅ Проверяйте на реальных устройствах
- ✅ Используйте screen reader для тестирования

### DON'T (Не делайте):

- ❌ Не используйте `outline: none` без альтернативы
- ❌ Не полагайтесь только на цвет для передачи информации
- ❌ Не создавайте элементы меньше 44×44px на mobile
- ❌ Не игнорируйте семантическую разметку
- ❌ Не пропускайте тестирование доступности

---

## Ресурсы

- [WCAG 2.2 Quick Reference](https://www.w3.org/WAI/WCAG22/quickref/)
- [ARIA Authoring Practices Guide](https://www.w3.org/WAI/ARIA/apg/)
- [WebAIM Contrast Checker](https://webaim.org/resources/contrastchecker/)
- [axe DevTools](https://www.deque.com/axe/devtools/)
- [WAVE Tool](https://wave.webaim.org/)

---

**Примечание**: Соблюдение этих правил обязательно для всех компонентов дизайн-системы.