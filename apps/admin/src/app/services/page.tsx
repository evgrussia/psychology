import React from 'react';
import { SectionPlaceholder } from '@/components/section-placeholder';

export default function ServicesPage() {
  return (
    <SectionPlaceholder
      title="Услуги"
      description="Список услуг, цены, депозит и правила бронирования."
    />
  );
}
