# Production environment — НЕ коммитить в git!
# Скопировать в .env.production на сервере и заполнить значения.

# Django
SECRET_KEY=<сгенерировать: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())">
ALLOWED_HOSTS=example.com,www.example.com,api.example.com
DJANGO_SETTINGS_MODULE=config.settings.production

# Database
DB_NAME=psychology_prod
DB_USER=psychology_user
DB_PASSWORD=<secure_password>
DB_HOST=localhost
DB_PORT=5432

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Email
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_HOST_USER=noreply@example.com
EMAIL_HOST_PASSWORD=<email_password>
DEFAULT_FROM_EMAIL=noreply@example.com

# External (опционально для MVP)
# YOOKASSA_SHOP_ID=
# YOOKASSA_SECRET_KEY=
# GOOGLE_CALENDAR_CLIENT_ID=
# GOOGLE_CALENDAR_CLIENT_SECRET=
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHANNEL_ID=

# Monitoring (опционально)
# JAEGER_HOST=localhost
# JAEGER_PORT=6831
