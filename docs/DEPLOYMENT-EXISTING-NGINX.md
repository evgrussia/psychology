# Деплой с существующим Nginx на сервере

Если на вашем сервере уже установлен Nginx и развернуты другие проекты, используйте эту инструкцию для безопасного добавления Psychology Platform.

## Архитектура

```
Internet
    ↓
Существующий Nginx на хосте (порт 80/443)
    ├── другие проекты...
    └── balance-space.ru → 127.0.0.1:8080 (Docker Nginx)
                              ↓
                         Psychology Platform
```

## Основные отличия

### ✅ Что изменено для безопасности:

1. **Docker Nginx НЕ использует порты 80/443** на хосте
2. **Внутренние порты**: 8080 (web+api) и 8081 (admin)
3. **SSL обрабатывает внешний Nginx** на хосте
4. **Изолированная сеть Docker** для проекта
5. **Отдельные логи** для Psychology Platform

---

## 🚀 Инструкция по деплою

### Шаг 1: Подготовка (без изменений)

```bash
# Настройте DNS записи
# Сгенерируйте пароли
openssl rand -base64 64  # JWT secret
openssl rand -base64 64  # Session secret
openssl rand -base64 32  # DB password
openssl rand -base64 32  # Redis password
```

### Шаг 2: Получение SSL сертификатов

Если сертификаты еще не получены:

```bash
# Остановите существующий Nginx на время получения сертификата
sudo systemctl stop nginx

# Получите сертификаты
sudo certbot certonly --standalone \
  -d balance-space.ru \
  -d www.balance-space.ru \
  -d admin.balance-space.ru \
  -d api.balance-space.ru \
  --email your@email.com \
  --agree-tos

# Запустите Nginx обратно
sudo systemctl start nginx
```

Или используйте webroot метод (если Nginx работает):

```bash
sudo certbot certonly --webroot \
  -w /var/www/html \
  -d balance-space.ru \
  -d www.balance-space.ru \
  -d admin.balance-space.ru \
  -d api.balance-space.ru \
  --email your@email.com \
  --agree-tos
```

### Шаг 3: Клонирование проекта

```bash
# От пользователя deploy
su - deploy
sudo mkdir -p /var/www/psychology
sudo chown deploy:deploy /var/www/psychology
cd /var/www/psychology

# Клонируйте репозиторий
git clone -b main YOUR_REPO_URL .
```

### Шаг 4: Настройка окружения

```bash
cp env.prod.example .env.prod
nano .env.prod
```

**Важно**: В `.env.prod` укажите правильные URL:
```env
API_URL=https://api.balance-space.ru
SITE_URL=https://balance-space.ru
ADMIN_URL=https://admin.balance-space.ru
NEXT_PUBLIC_API_URL=https://balance-space.ru/api
NEXT_PUBLIC_SITE_URL=https://balance-space.ru
NEXT_PUBLIC_ADMIN_URL=https://admin.balance-space.ru
```

### Шаг 5: Настройка внешнего Nginx

```bash
# Скопируйте конфигурацию для внешнего Nginx
sudo cp /var/www/psychology/nginx/host-nginx-config.conf \
  /etc/nginx/sites-available/balance-space.ru

# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/balance-space.ru \
  /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Если ошибок нет, перезагрузите Nginx
sudo systemctl reload nginx
```

### Шаг 6: Деплой Docker контейнеров

```bash
cd /var/www/psychology
chmod +x scripts/*.sh

# Запустите деплой
./scripts/deploy.sh deploy
```

Скрипт автоматически:
- Соберет Docker образы
- Запустит контейнеры на портах 8080 и 8081
- Применит миграции БД
- Проверит здоровье сервисов

### Шаг 7: Проверка

```bash
# Проверьте Docker контейнеры
docker ps | grep psychology

# Проверьте локальные порты
curl http://127.0.0.1:8080/health  # Web
curl http://127.0.0.1:8081/health  # Admin (может не работать)

# Проверьте через домен
curl https://balance-space.ru/api/health
curl https://admin.balance-space.ru/
```

---

## 📋 Проверка конфликтов портов

### Убедитесь, что порты 8080 и 8081 свободны:

```bash
# Проверьте занятые порты
sudo netstat -tulpn | grep -E '8080|8081'

# Или используйте ss
sudo ss -tulpn | grep -E '8080|8081'
```

Если порты заняты, измените их в `docker-compose.prod.yml`:

```yaml
nginx:
  ports:
    - "127.0.0.1:8090:80"  # Вместо 8080
    - "127.0.0.1:8091:81"  # Вместо 8081
```

И обновите конфигурацию внешнего Nginx:

```nginx
upstream psychology_web {
    server 127.0.0.1:8090;  # Новый порт
}

upstream psychology_admin {
    server 127.0.0.1:8091;  # Новый порт
}
```

---

## 🔧 Управление проектом

### Все команды работают так же:

```bash
# Логи
./scripts/deploy.sh logs

# Статус
./scripts/deploy.sh status

# Перезапуск
./scripts/deploy.sh restart

# Остановка (НЕ затронет другие проекты)
./scripts/deploy.sh stop

# Мониторинг
./scripts/monitor.sh
```

---

## 🛡️ Изоляция и безопасность

### Что изолировано:

✅ **Порты**: Psychology Platform использует 8080/8081, не конфликтует с 80/443  
✅ **Docker сеть**: Отдельная сеть `psychology-network`  
✅ **Volumes**: Отдельные volumes для БД, Redis, медиа  
✅ **Логи**: Отдельные лог-файлы в `/var/log/nginx/psychology-*`  
✅ **Процессы**: Все сервисы изолированы в Docker контейнерах  

### Что НЕ влияет на другие проекты:

- Остановка/перезапуск Psychology Platform
- Обновление кода
- Миграции базы данных
- Изменение конфигурации

### Единственная точка пересечения:

**Внешний Nginx** на хосте - он проксирует трафик. Но конфигурация для Psychology Platform находится в отдельном файле `balance-space.ru` и не влияет на другие виртуальные хосты.

---

## 🔄 Обновление проекта

```bash
cd /var/www/psychology
./scripts/deploy.sh deploy
```

Это **НЕ затронет**:
- Другие проекты на сервере
- Другие виртуальные хосты Nginx
- Другие Docker контейнеры

---

## 🆘 Troubleshooting

### Проблема: `Error: Cannot find module '/app/server.js'`

**Причина**: контейнер `web` или `admin` запускается командой `node server.js`, но в standalone-сборке Next.js (особенно в монорепах) `server.js` может оказаться не в `/app/server.js`, а в `/app/apps/web/server.js` или `/app/apps/admin/server.js`.

**Решение**:
- Обновите репозиторий на сервере до версии, где исправлены Dockerfile (`apps/web/Dockerfile`, `apps/admin/Dockerfile`), и выполните пересборку:

```bash
cd /var/www/psychology
git pull
./scripts/deploy.sh deploy
```

### Проблема: Ошибка "address already in use"

**Решение**: Измените порты в `docker-compose.prod.yml`:

```yaml
nginx:
  ports:
    - "127.0.0.1:8090:80"
    - "127.0.0.1:8091:81"
```

### Проблема: Nginx не перезагружается

**Решение**: Проверьте конфигурацию:

```bash
sudo nginx -t

# Если есть ошибки, исправьте их
sudo nano /etc/nginx/sites-available/balance-space.ru

# Проверьте снова
sudo nginx -t

# Перезагрузите
sudo systemctl reload nginx
```

### Проблема: 502 Bad Gateway

**Решение**: Проверьте, что Docker контейнеры запущены:

```bash
docker ps | grep psychology

# Если контейнеры не запущены
cd /var/www/psychology
./scripts/deploy.sh restart
```

### Проблема: Конфликт с существующим SSL

**Решение**: Используйте существующие сертификаты:

```bash
# Проверьте существующие сертификаты
sudo certbot certificates

# Используйте их в конфигурации Nginx
```

---

## 📊 Сравнение с standalone деплоем

| Аспект | Standalone | С существующим Nginx |
|--------|-----------|---------------------|
| Порты 80/443 | Docker Nginx | Внешний Nginx на хосте |
| SSL | Внутри Docker | На хосте |
| Изоляция | Полная | Полная (кроме Nginx) |
| Конфликты | Невозможны | Минимальные (только порты) |
| Сложность | Проще | Чуть сложнее |
| Гибкость | Меньше | Больше |

---

## ✅ Чеклист деплоя

- [ ] DNS записи настроены
- [ ] SSL сертификаты получены
- [ ] Порты 8080/8081 свободны (или выбраны другие)
- [ ] Проект склонирован
- [ ] `.env.prod` настроен
- [ ] Конфигурация внешнего Nginx добавлена
- [ ] Nginx конфигурация проверена (`nginx -t`)
- [ ] Nginx перезагружен
- [ ] Docker контейнеры запущены
- [ ] Проверка через curl работает
- [ ] Сайт открывается в браузере
- [ ] Другие проекты продолжают работать

---

## 📚 Дополнительные ресурсы

- [Основная инструкция](./DEPLOYMENT.md) - для standalone деплоя
- [Быстрые команды](./COMMANDS.md) - справочник команд
- [Чеклист](./DEPLOYMENT-CHECKLIST.md) - полный чеклист

---

**Готово!** Ваш проект будет работать изолированно, не влияя на другие проекты на сервере. 🎉
