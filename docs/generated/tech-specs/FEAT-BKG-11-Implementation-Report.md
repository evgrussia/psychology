# Отчет о реализации FEAT-BKG-11

**Дата проверки:** 2026-01-17  
**Дата обновления:** 2026-01-17  
**Статус:** ✅ Полностью реализовано  
**Версия спеки:** v0.1 (draft)

---

## Резюме

Реализованы автоматические альтернативы для сценария “нет слотов”: ближайшие слоты за пределами базового окна, ближайшие доступные дни, подсказки по времени, а также альтернативы по формату/услугам. Логика не создаёт конфликтов и опирается на существующий механизм анти‑гонок при бронировании.

---

## ✅ Реализованные требования (AC/DoD)

> Примечание: в спецификации FEAT‑BKG‑11 отсутствует секция AC/DoD, поэтому требования выведены из Summary и DoD в задаче.

- **AC‑1:** При отсутствии слотов в базовом диапазоне показываются ближайшие доступные слоты в расширенном диапазоне.
- **AC‑2:** Показываются ближайшие доступные дни с количеством слотов и первым временем.
- **AC‑3:** Предлагаются другие временные окна (будни утро/день, будни вечер, выходные).
- **AC‑4:** Предлагаются онлайн/офлайн альтернативы через сервисы той же темы или смену формата для hybrid.
- **DoD:** Логика не создаёт конфликтов — слот резервируется только через `booking/start`, конфликт отрабатывается как `slot_conflict`.

---

## 🔧 Ключевые точки реализации

### Backend
- `apps/api/src/application/booking/use-cases/GetBookingAlternativesUseCase.ts` — расчет альтернатив, окна, дни, форматы.
- `apps/api/src/application/booking/dto/alternatives.dto.ts` — контракт ответа.
- `apps/api/src/presentation/controllers/public/public.controller.ts` — `GET /public/booking/alternatives`.

### Frontend
- `apps/web/src/app/booking/no-slots/NoSlotsClient.tsx` — UI альтернатив, выбор слота, обработка конфликтов.

---

## ✅ Тестирование

Добавлены интеграционные тесты:
- `apps/api/src/application/booking/use-cases/GetBookingAlternativesUseCase.spec.ts`

Команда для запуска:
```
pnpm --filter api test -- GetBookingAlternativesUseCase
```

---

## ⚠️ Блокеры / ограничения

- Нет блокеров.
- Спецификация FEAT‑BKG‑11 остаётся draft и не содержит формализованных AC — приняты выводимые критерии из Summary и DoD задачи.

