# Screens, Templates, and App Components

Эта документация описывает новые секции Design System, созданные для разработки реальных приложений с использованием единых конвенций и существующих токенов/компонентов.

---

## 📱 Screens (Web)

**Путь:** `/src/app/components/ScreensWeb.tsx`

### Описание

Полноценные экраны приложения "Эмоциональный баланс" в формате Mobile (375px) и Desktop (1440px). Каждый экран показывает реальное применение компонентов, паттернов и токенов дизайн-системы.

### Конвенции именования

Все экраны следуют единой схеме именования:

```
Route — State — Breakpoint
```

**Примеры:**
- `Home / Landing — Default — Mobile`
- `Home / Landing — Default — Desktop`
- `Quizzes / List — Default — Mobile`
- `Quizzes / [slug] — Progress — Desktop`
- `Quizzes / [slug] — Result — Mobile`
- `Dashboard / Profile — Default — Desktop`

### Список экранов

1. **Home / Landing — Default**
   - Hero-секция с приветствием
   - Статистика (Quick Stats)
   - Популярные темы (Topic Cards)
   - Bottom Navigation (Mobile) / Top Navigation (Desktop)

2. **Quizzes / List — Default**
   - Каталог тестов с поиском
   - Фильтры по категориям (Tabs)
   - Grid карточек тестов
   - Информация о длительности и количестве вопросов

3. **Quizzes / [slug] — Progress**
   - Progress Bar (Вопрос X из Y)
   - QuizCard компонент (single-choice/multi-choice/scale)
   - Навигация "Назад"

4. **Quizzes / [slug] — Result**
   - Иконка успешного завершения
   - Результат (Score Display)
   - Интерпретация результатов
   - Рекомендации
   - CTA кнопки (Сохранить/Пройти ещё раз)

5. **Dashboard / Profile — Default**
   - Профиль пользователя
   - Статистика (Stats Grid)
   - Недавняя активность
   - График настроения (Mood Trend)

### Брейкпоинты

- **Mobile:** 375px (iPhone SE/8)
- **Desktop:** 1440px (Full HD)

Используется переключатель viewport для быстрого просмотра обоих версий.

### Используемые компоненты

- Все UI компоненты из `/src/app/components/ui/`
- Доменные компоненты: `QuizCard`, `MoodCheckIn`
- Icons из `lucide-react`
- Цветовые токены и spacing из `theme.css`

---

## 🎨 Components (App)

**Путь:** `/src/app/components/AppComponents.tsx`

### Описание

Прикладные компоненты, собранные ИСКЛЮЧИТЕЛЬНО из существующих токенов, стилей и базовых UI-компонентов. Эти компоненты готовы к использованию в реальных экранах приложения.

### Список компонентов

#### 1. Hero Sections

- **Hero - Primary:** Полноценная hero-секция для лендингов
  - Badge + Заголовок + Описание + CTA кнопки + Метрики
  - Градиентный фон (primary/accent/secondary)
  
- **Hero - Compact:** Компактный вариант для внутренних страниц
  - Заголовок + Описание + Одна CTA кнопка

#### 2. Feature Blocks

Grid функций/преимуществ (3 колонки на desktop):
- Иконка в цветном контейнере
- Заголовок
- Описание
- Цветовые варианты: primary, success, warning

#### 3. Topic Cards

Карточки тем, курсов и категорий:

- **Topic Card - Image:** С изображением (aspect-video)
  - Badge (Новое/Популярное)
  - Метрики (количество уроков, рейтинг)
  - Hover-эффекты
  
- **Topic Card - Icon:** С иконкой
  - Progress Bar
  - CTA кнопка "Продолжить обучение"

#### 4. Content Cards

Карточки для контента (статьи, видео, курсы):

- **Article Card:** Badge "Статья" + Мета-информация (время чтения, дата)
- **Video Card:** Preview с Play иконкой + Duration badge
- **Course Card:** Progress Bar + Badge "Курс" + CTA

#### 5. Stats Widgets

Grid из 4 виджетов статистики:
- Иконка + Значение + Метка + Trend
- Цветовые варианты: primary, success, warning, info

#### 6. CTA Blocks

- **CTA - Primary:** Градиентный блок с заголовком, описанием и CTA
- **CTA - Secondary:** Центрированный блок с иконкой, заголовком и двумя кнопками

#### 7. Testimonials

Карточки отзывов пользователей:
- Рейтинг (звёзды)
- Текст отзыва
- Имя автора + роль
- Градиентный фон

#### 8. FAQ Items

Expandable FAQ элементы:
- Вопрос + ChevronRight иконка
- Expandable ответ
- Hover-эффекты

### Принципы создания

✅ **Используются только существующие токены:**
- Цвета: `primary`, `success`, `warning`, `danger`, `info`, `muted`, `accent`
- Spacing: `space-2`, `space-4`, `space-6`, `space-8`, etc.
- Radius: `radius-md`, `radius-lg`, `radius-xl`
- Elevation: `elevation-1`, `elevation-2`, etc.

✅ **Собраны из базовых UI-компонентов:**
- `Card`, `CardContent`, `CardHeader`, `CardTitle`
- `Button`, `Badge`, `Progress`
- Icons из `lucide-react`

✅ **Готовы к импорту в приложение:**
```tsx
import { AppComponents } from '@/app/components/AppComponents';
```

---

## 📄 Templates

**Путь:** `/src/app/components/Templates.tsx`

### Описание

Полноценные шаблоны страниц, собранные из прикладных и базовых компонентов. Готовы к использованию в качестве стартовых точек для создания новых экранов.

### Список шаблонов

#### Template 1: Marketing / Content Page

**Структура:**
1. **Hero Section**
   - Badge + Заголовок H1 + Описание
   - 2 CTA кнопки (Primary + Secondary)
   - Метрики (10,000+ пользователей, 4.8 рейтинг)

2. **Content/Prose Section**
   - Feature Grid (3 колонки)
   - Текстовый блок с заголовком H2
   - Список с иконками (CheckCircle2)
   - Testimonials Grid (2 колонки)

3. **CTA Block**
   - Заголовок H2 + Описание
   - CTA кнопка

**Применение:** Лендинги, маркетинговые страницы, презентации продукта

---

#### Template 2: Catalog / List Page

**Структура:**
1. **Header Section**
   - Заголовок H1 + Описание
   - Search Bar
   - Filter Button

2. **Filters/Tags Section**
   - Pill-кнопки для категорий
   - Активная категория выделена primary цветом

3. **Grid/List of Cards**
   - Grid 3 колонки (desktop) / 1 колонка (mobile)
   - Карточки с иконкой, заголовком, описанием
   - Метрики (время, количество)
   - Hover-эффекты (ChevronRight иконка)

**Применение:** Каталоги тестов, библиотеки контента, списки материалов

---

#### Template 3: Interactive Flow

**Структура из 3 шагов:**

**Шаг 1: Start Screen**
- Иконка в цветном контейнере
- Badge с мета-информацией
- Заголовок H2 + Описание
- Список "Что вы получите" (CheckCircle2)
- 2 CTA кнопки

**Шаг 2: Progress Screen**
- Progress Header (Вопрос X из Y)
- Progress Bar
- QuizCard компонент (single-choice/multi-choice/scale)
- Кнопка "Назад"

**Шаг 3: Result Screen**
- Success иконка
- Заголовок "Тест завершён!"
- Score Display (gradient card)
- Интерпретация результатов (Card)
- Рекомендации (Card со списком)
- 2 CTA кнопки (Сохранить / Пройти ещё раз)

**Применение:** Квизы, тесты, опросы, многошаговые формы

---

## 🎯 Конвенции и Best Practices

### Структура файлов

```
/src/app/components/
├── ui/                      # Базовые UI компоненты (shadcn/ui)
├── domain/                  # Доменные компоненты (QuizCard, MoodCheckIn, etc.)
├── AppComponents.tsx        # Прикладные компоненты
├── ScreensWeb.tsx          # Экраны приложения
└── Templates.tsx           # Шаблоны страниц
```

### Именование

- **Screens:** `Route — State — Breakpoint`
- **Components:** `ComponentName` (PascalCase)
- **Props:** `componentNameProps` (camelCase)

### Импорты

Используйте alias `@` для импортов:

```tsx
import { Button } from '@/app/components/ui/button';
import { QuizCard } from '@/app/components/domain/QuizCard';
```

### Токены и стили

❌ **Не используйте:**
- Hardcoded цвета: `#17A2B8`, `rgb(23, 162, 184)`
- Hardcoded spacing: `16px`, `24px`
- Inline styles: `style={{ color: '#000' }}`

✅ **Используйте:**
- CSS-переменные: `text-primary`, `bg-accent`, `border-border`
- Tailwind spacing: `p-4`, `gap-6`, `space-y-8`
- Semantic tokens: `text-foreground`, `bg-background`, `text-muted-foreground`

### Responsive Design

Все компоненты и экраны должны быть responsive:

```tsx
// Mobile-first подход
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
```

### Accessibility (WCAG 2.2 AA)

- Все интерактивные элементы имеют `min-height: 44px` (mobile)
- Корректный контраст текста и фона
- Focus states видны для клавиатурной навигации
- Правильная семантика HTML (h1, h2, nav, etc.)

---

## 🚀 Использование

### Как добавить новый экран

1. Откройте `/src/app/components/ScreensWeb.tsx`
2. Добавьте новую секцию с экраном
3. Следуйте конвенции именования: `Route — State — Breakpoint`
4. Используйте существующие компоненты из `AppComponents.tsx`
5. Применяйте токены из `theme.css`

### Как создать новый прикладной компонент

1. Откройте `/src/app/components/AppComponents.tsx`
2. Добавьте новую секцию
3. Соберите компонент из базовых UI-элементов
4. Используйте только токены из Design System
5. Добавьте hover/active states для интерактивности

### Как использовать шаблон

1. Откройте `/src/app/components/Templates.tsx`
2. Найдите нужный шаблон (Template 1/2/3)
3. Скопируйте структуру
4. Замените контент на реальные данные
5. Адаптируйте под ваши нужды

---

## 📝 Changelog

### Version 1.1.0

**Добавлено:**
- Секция "Screens (Web)" с 5 экранами
- Секция "Components (App)" с 8 категориями компонентов
- Обновлённая секция "Templates" с 3 полноценными шаблонами
- Документация SCREENS_TEMPLATES_GUIDE.md

**Компоненты:**
- Hero Sections (Primary, Compact)
- Feature Blocks
- Topic Cards (Image, Icon)
- Content Cards (Article, Video, Course)
- Stats Widgets
- CTA Blocks (Primary, Secondary)
- Testimonials
- FAQ Items

**Экраны:**
- Home / Landing — Default — Mobile/Desktop
- Quizzes / List — Default — Mobile/Desktop
- Quizzes / [slug] — Progress — Mobile/Desktop
- Quizzes / [slug] — Result — Mobile/Desktop
- Dashboard / Profile — Default — Mobile/Desktop

**Шаблоны:**
- Template 1: Marketing / Content Page
- Template 2: Catalog / List Page
- Template 3: Interactive Flow (Start → Progress → Result)

---

## 🔗 См. также

- [Design System Documentation](./DESIGN_SYSTEM.md)
- [Tokens Reference](./TOKENS.md)
- [Accessibility Guidelines](./ACCESSIBILITY.md)
- [Developer Guide](./DEVELOPER_GUIDE.md)
