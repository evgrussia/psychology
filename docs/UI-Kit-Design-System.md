# Design System — «Эмоциональный баланс» (v2)

**Версия:** v2  
**Дата:** Январь 2026  
**Источник истины:** репозиторий, папка `design-system/`  
**Статус:** ✅ актуально

---

## 📌 Обзор

Новая дизайн‑система полностью заменяет старую и построена на современных технологиях:
- **Tailwind CSS v4** — основной движок стилизации.
- **CSS Custom Properties (design tokens)** — определены в `design-system/src/styles/theme.css`.
- **Light/Dark темы** — поддерживаются через класс `.dark`.
- **UI‑компоненты** — на базе Radix UI и Headless UI.

---

## 🧱 Структура (в пакете `@psychology/design-system`)

- **`/src/components/ui`** — базовые атомарные компоненты (Button, Card, Input, Sidebar и т.д.).
- **`/src/components/shared`** — прикладные блоки (HeroSection, CTABlock, FAQSection, TrustBlocks).
- **`/src/components/domain`** — компоненты, привязанные к бизнес-логике (QuizCard, MoodCheckIn).
- **`/src/styles`** — CSS-файлы (theme.css, tailwind.css, fonts.css).
- **`/dist`** — собранные артефакты (JS и CSS).

---

## ✅ Правила использования

### 1. Стилизация
Используйте только Tailwind-классы или CSS-переменные из темы. Избегайте хардкода значений.
```tsx
// ✅ Правильно
<div className="bg-muted p-4 rounded-xl text-primary" />

// ✅ Правильно (через переменные)
<div style={{ color: 'var(--primary)' }} />

// ❌ Неправильно
<div style={{ color: '#5A7A5E', padding: '16px' }} />
```

### 2. Импорты
Все компоненты экспортируются из корня пакета. Стили подключаются один раз в layout приложения.

```tsx
// В layout.tsx
import '@psychology/design-system/styles.css';

// В компонентах
import { Button, Card, HeroSection } from '@psychology/design-system';
```

### 3. Темы
Приложение должно использовать `next-themes` или аналогичный механизм для переключения класса `.dark` на теге `html`.

---

## 🔗 Где смотреть компоненты

Полный список доступных компонентов и примеров их использования можно найти в исходниках:
- `design-system/src/index.ts` — список всех экспортов.
- `design-system/src/components/shared/*` — прикладные блоки.

---

## 🛠️ Разработка и сборка

Если вы вносите изменения в `design-system`:
1. Внесите правки в нужные файлы в `src/`.
2. Запустите сборку: `pnpm --filter @psychology/design-system build`.
3. Стили в приложениях обновятся автоматически (через workspace-ссылку).

---

**Последнее обновление:** 15 января 2026  
**Статус:** ✅ Готово к использованию в `apps/web` и `apps/admin`
