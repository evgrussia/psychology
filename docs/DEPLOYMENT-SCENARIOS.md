# Варианты деплоя Psychology Platform

## 🔄 Два сценария деплоя

В зависимости от состояния вашего сервера, выберите подходящий вариант:

---

## Вариант А: Чистый сервер (Standalone)

### Когда использовать:
- ✅ Новый чистый VPS/сервер
- ✅ На сервере НЕТ других проектов
- ✅ Nginx НЕ установлен
- ✅ Вы хотите простое решение

### Архитектура:
```
Internet → Docker Nginx (порты 80/443) → Psychology Platform
```

### Особенности:
- Docker Nginx слушает порты 80 и 443 напрямую
- SSL настраивается внутри Docker
- Полная изоляция
- Проще в настройке

### Инструкции:
- **Полная**: [docs/DEPLOYMENT.md](./docs/DEPLOYMENT.md)
- **Краткая**: [docs/QUICK-DEPLOY.md](./docs/QUICK-DEPLOY.md)

### Команды:
```bash
# Настройка сервера
./scripts/setup-server.sh

# SSL внутри Docker (не нужен внешний Nginx)
# Деплой
./scripts/deploy.sh deploy
```

---

## Вариант Б: Существующий Nginx

### Когда использовать:
- ✅ На сервере УЖЕ установлен Nginx
- ✅ Есть другие развернутые проекты
- ✅ Порты 80/443 заняты
- ✅ Нужна совместимость с текущей инфраструктурой

### Архитектура:
```
Internet → Nginx на хосте (80/443) → Docker Nginx (8080/8081) → Psychology Platform
                ├── другие проекты...
                └── balance-space.ru
```

### Особенности:
- Docker Nginx слушает локальные порты 8080/8081
- SSL настраивается на внешнем Nginx
- Полная изоляция от других проектов
- Чуть сложнее в настройке

### Инструкция:
- **Полная**: [docs/DEPLOYMENT-EXISTING-NGINX.md](./docs/DEPLOYMENT-EXISTING-NGINX.md)

### Команды:
```bash
# Настройка проекта
cd /var/www/psychology
./scripts/deploy.sh deploy

# Добавление конфигурации в существующий Nginx
sudo cp nginx/host-nginx-config.conf /etc/nginx/sites-available/balance-space.ru
sudo ln -s /etc/nginx/sites-available/balance-space.ru /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

---

## Сравнение вариантов

| Характеристика | Вариант А (Чистый) | Вариант Б (Существующий) |
|----------------|-------------------|-------------------------|
| **Сложность** | Проще | Чуть сложнее |
| **Время настройки** | 30 минут | 40 минут |
| **Порты** | 80, 443 | 8080, 8081 (локальные) |
| **SSL** | Внутри Docker | На хосте |
| **Изоляция** | Полная | Полная |
| **Конфликты** | Невозможны | Минимальны (только порты) |
| **Гибкость** | Меньше | Больше |
| **Масштабируемость** | Средняя | Высокая |

---

## Что общего?

### Оба варианта включают:
- ✅ Docker Compose для изоляции
- ✅ PostgreSQL + Redis в контейнерах
- ✅ Автоматические миграции БД
- ✅ Healthchecks всех сервисов
- ✅ Автоматическое резервное копирование
- ✅ Скрипты управления
- ✅ Мониторинг

### Одинаковые команды:
```bash
./scripts/deploy.sh deploy   # Деплой
./scripts/deploy.sh logs     # Логи
./scripts/deploy.sh restart  # Перезапуск
./scripts/deploy.sh backup   # Бэкап
./scripts/monitor.sh         # Мониторинг
```

---

## Выбор варианта

### Выбирайте Вариант А, если:
- 🟢 У вас новый сервер
- 🟢 Вы не планируете другие проекты
- 🟢 Хотите быстрое решение
- 🟢 Не знакомы с Nginx

### Выбирайте Вариант Б, если:
- 🟢 На сервере уже есть Nginx
- 🟢 Развернуты другие проекты
- 🟢 Нужна максимальная изоляция
- 🟢 Планируете добавлять другие проекты

---

## Миграция между вариантами

### Из А в Б (добавились другие проекты):
1. Установите Nginx на хост
2. Измените порты в `docker-compose.prod.yml` (80→8080, 443→8081)
3. Добавьте конфигурацию внешнего Nginx
4. Перезапустите контейнеры

### Из Б в А (сервер освободился):
1. Удалите конфигурацию из внешнего Nginx
2. Измените порты в `docker-compose.prod.yml` (8080→80, 8081→443)
3. Остановите внешний Nginx
4. Перезапустите контейнеры

---

## Рекомендации

### Для production:
👉 **Вариант Б** - лучшая практика, больше гибкости

### Для dev/staging:
👉 **Вариант А** - быстрее и проще

### Для общего хостинга:
👉 **Вариант Б** - обязательно, не конфликтует

---

## Быстрый старт

### Определите ваш случай:
```bash
# Проверьте, установлен ли Nginx
which nginx

# Проверьте, заняты ли порты
sudo netstat -tulpn | grep -E ':80|:443'

# Проверьте другие проекты
ls /etc/nginx/sites-enabled/
```

### Если команды выше ничего не показали:
👉 **Используйте Вариант А**

### Если Nginx установлен или порты заняты:
👉 **Используйте Вариант Б**

---

## Дополнительная помощь

- **Вопросы по деплою**: см. раздел Troubleshooting в соответствующей инструкции
- **Изменение портов**: см. `docs/DEPLOYMENT-EXISTING-NGINX.md`
- **Миграция данных**: см. `docs/COMMANDS.md`

---

**Выберите подходящий вариант и следуйте инструкции!** 🚀
